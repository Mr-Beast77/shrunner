name: GitHub Actions Demo
run-name: Testing
on: [push]
jobs:
  Test:
    name: test
    runs-on: alpine/curl:latest
    steps:
      - run: cat /etc/os*
      - run: env
  Build:
    name: build
    needs: Test
    runs-on: ubuntu-latest
    steps:
      - run: echo I am job 2; date
  Post-Build:
    name: post build
    needs:  Build
    runs-on: ubuntu-latest
    steps:
      - run: echo I am job 3; date
